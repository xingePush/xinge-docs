# 发布流程

1. 先从 gitbook 的仓库下面把最新的代码拉到本地的 tmp 分支
`git remote add gitbook https://git.gitbook.com/tencenbigdata/xg-docs.git`
`git fetch gitbook master:tmp`
ps: 用户名/密码 TencenBigData/tencentbigdata

2. 合并本地的 master 分支和 tmp 分支
`git checkout master`
`git merge tmp`

3. 本地构建 gitbook 静态文件
`gitbook build`

4. 提交到 git.oa ，然后发布
